import React from "react";
import { Loader, LoaderWrapper } from "../styles/loaderStyles";

const FullScreenLoader = () => {
  return (
    <LoaderWrapper>
      <Loader />
    </LoaderWrapper>
  );
};

export default FullScreenLoader;
